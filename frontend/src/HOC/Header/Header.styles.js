import styled from "styled-components";

export const StyledHeader = styled.header`
  & > svg {
    width: 17rem;
    height: 5.6rem;
    flex-shrink: 0;
    cursor: pointer;
  }

  button svg {
    width: 3.2rem;
    height: 3.2rem;
  }
`;
